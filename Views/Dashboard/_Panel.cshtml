@* File: Views/Dashboard/_Panel.cshtml *@
@model dynamic

<div class="bg-white border border-gray-200 rounded-lg p-6">
    @if (ViewBag.Title != null)
    {
        <h3 class="text-lg font-semibold text-gray-900 mb-4">@ViewBag.Title</h3>
    }
    
    <div class="space-y-3">
        @RenderBody()
    </div>
    
    @if (ViewBag.FooterLink != null && ViewBag.FooterText != null)
    {
        <div class="mt-4 pt-4 border-t border-gray-100">
            <a href="@ViewBag.FooterLink" class="text-sm text-blue-600 hover:text-blue-700 font-medium">
                @ViewBag.FooterText →
            </a>
        </div>
    }
</div>