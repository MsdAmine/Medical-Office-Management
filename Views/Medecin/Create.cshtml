@model MedicalOfficeManagement.Models.Medecin

<div class="page-shell">
    <div class="page-header">
        <div>
            <div class="inline-flex items-center gap-2 rounded-full bg-brand-50 text-brand-700 px-3 py-1 text-xs font-semibold uppercase tracking-wide">Staff</div>
            <h1 class="page-title mt-2">Add New Staff Member</h1>
        </div>
        <a asp-action="Index" class="btn-secondary">Cancel</a>
    </div>

    <div class="surface p-8 md:p-10 space-y-6">
        @* The validation summary is key to seeing remaining issues *@
        <div asp-validation-summary="All" class="rounded-xl bg-red-50 p-4 text-sm text-red-600 font-bold mb-4"></div>

        <form asp-action="Create" method="post" class="space-y-6">
            <div class="form-field">
                <label class="form-label">Account Type</label>
                <select name="SelectedRole" id="roleSelector" class="form-input">
                    <option value="Medecin">Doctor</option>
                    <option value="Personnel">Secretary (Personnel)</option>
                </select>
            </div>

            <div class="form-grid">
                <div class="form-field">
                    <label class="form-label">Full Name</label>
                    <input asp-for="NomPrenom" class="form-input" required />
                </div>
                <div class="form-field">
                    <label class="form-label">Email Address</label>
                    <input asp-for="Email" class="form-input" required />
                </div>
            </div>

            <div id="doctorSection" class="space-y-6">
                @* Fix for image_d0ba6e.png: Address field for Doctors *@
                <div class="form-field">
                    <label class="form-label">Office Address</label>
                    <input asp-for="Adresse" class="form-input" placeholder="Enter clinic address..." />
                </div>

                <div class="form-grid">
                    <div class="form-field">
                        <label class="form-label">Medical Specialty</label>
                        <select asp-for="Specialite" asp-items="ViewBag.Specialites" class="form-input"></select>
                    </div>
                    <div class="form-field">
                        <label class="form-label">Office Phone</label>
                        <input asp-for="Telephone" class="form-input" />
                    </div>
                </div>
            </div>

            <div id="secretarySection" style="display:none;" class="form-grid">
                <div class="form-field">
                    <label class="form-label">Direct Phone Line</label>
                    <input name="TelephonePersonnel" class="form-input" />
                </div>
                <div class="form-field">
                    <label class="form-label">Department</label>
                    <input name="ServicePersonnel" class="form-input" />
                </div>
            </div>

            <div class="flex justify-end gap-3 pt-4">
                <a asp-action="Index" class="btn-secondary">Back</a>
                <button type="submit" class="btn-primary">Create Account</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('roleSelector').addEventListener('change', function () {
            const isDoctor = this.value === 'Medecin';
            document.getElementById('doctorSection').style.display = isDoctor ? 'block' : 'none';
            document.getElementById('secretarySection').style.display = isDoctor ? 'none' : 'grid';
        });
    </script>
}