@model PrescriptionIndexViewModel

@{
    ViewData["Title"] = "Prescriptions";
    ViewData["Breadcrumb"] = "Prescriptions";
}

<div class="space-y-6">
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
        <div>
            <h1 class="text-2xl lg:text-3xl font-bold text-slate-900">Prescriptions</h1>
            <p class="text-slate-600 mt-2">Track active, pending, and completed orders</p>
        </div>
        <div class="flex items-center gap-3">
            <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                    <i class="fas fa-search text-slate-400"></i>
                </div>
                <input id="prescriptionSearch"
                       type="search"
                       placeholder="Search prescriptions..."
                       class="pl-10 pr-4 py-2 border border-slate-300 rounded-lg w-full md:w-72
                              focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
            </div>
            <a asp-action="Create" class="gradient-primary text-white px-4 py-2 rounded-lg font-medium hover:shadow-lg transition-shadow flex items-center">
                <i class="fas fa-plus mr-2"></i>
                New Prescription
            </a>
            <a asp-action="Export" class="inline-flex items-center px-4 py-2 rounded-lg border border-slate-300 text-slate-700 font-medium hover:bg-slate-50 transition">
                <i class="fas fa-cloud-download-alt mr-2 text-slate-500"></i>
                Export
            </a>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="glass-card p-4 rounded-xl">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-slate-500">Active</p>
                    <p class="text-2xl font-bold text-slate-900">@Model.ActiveCount</p>
                </div>
                <div class="h-10 w-10 rounded-lg bg-blue-100 text-blue-600 flex items-center justify-center">
                    <i class="fas fa-check-circle"></i>
                </div>
            </div>
        </div>
        <div class="glass-card p-4 rounded-xl">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-slate-500">Pending</p>
                    <p class="text-2xl font-bold text-slate-900">@Model.PendingCount</p>
                </div>
                <div class="h-10 w-10 rounded-lg bg-amber-100 text-amber-600 flex items-center justify-center">
                    <i class="fas fa-hourglass-half"></i>
                </div>
            </div>
        </div>
        <div class="glass-card p-4 rounded-xl">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm text-slate-500">Completed</p>
                    <p class="text-2xl font-bold text-slate-900">@Model.CompletedCount</p>
                </div>
                <div class="h-10 w-10 rounded-lg bg-emerald-100 text-emerald-600 flex items-center justify-center">
                    <i class="fas fa-clipboard-check"></i>
                </div>
            </div>
        </div>
    </div>

    <div class="glass-card rounded-xl overflow-hidden">
        <div class="p-6 border-b border-slate-200 flex items-center justify-between">
            <div>
                <h2 class="text-lg font-semibold text-slate-900">Prescription List</h2>
                <p class="text-sm text-slate-600">Showing @Model.Prescriptions.Count() prescription(s)</p>
            </div>
            <div class="flex items-center gap-2 text-sm text-slate-500">
                <span class="flex items-center gap-1"><span class="badge-dot bg-blue-500"></span> Active</span>
                <span class="flex items-center gap-1"><span class="badge-dot bg-amber-500"></span> Pending</span>
                <span class="flex items-center gap-1"><span class="badge-dot bg-emerald-500"></span> Completed</span>
            </div>
        </div>

        <div class="overflow-x-auto">
            <table class="w-full">
                <thead>
                    <tr class="bg-slate-50 border-b border-slate-200">
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Patient</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Medication</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Prescriber</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Issued</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Refill</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Status</th>
                        <th class="text-left py-4 px-6 text-sm font-semibold text-slate-900">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-200">
                    @foreach (var prescription in Model.Prescriptions)
                    {
                        var statusColor = prescription.Status switch
                        {
                            "Active" => "bg-blue-100 text-blue-700",
                            "Pending" => "bg-amber-100 text-amber-700",
                            "Completed" => "bg-emerald-100 text-emerald-700",
                            _ => "bg-slate-100 text-slate-700"
                        };

                        <tr class="hover:bg-slate-50 transition-colors">
                            <td class="py-4 px-6">
                                <div class="font-semibold text-slate-900">@prescription.PatientName</div>
                                <div class="text-sm text-slate-500">#@prescription.Id</div>
                            </td>
                            <td class="py-4 px-6">
                                <div class="font-medium text-slate-900">@prescription.Medication</div>
                                <div class="text-sm text-slate-500">@prescription.Dosage · @prescription.Frequency</div>
                            </td>
                            <td class="py-4 px-6 text-slate-700">@prescription.PrescribedBy</td>
                            <td class="py-4 px-6 text-slate-700">@prescription.IssuedOn.ToString("MMM dd, yyyy")</td>
                            <td class="py-4 px-6 text-slate-700">
                                @if (prescription.NextRefill.HasValue)
                                {
                                    <span>@prescription.NextRefill.Value.ToString("MMM dd")</span>
                                    <span class="block text-xs text-slate-500">@prescription.RefillsRemaining refill(s) left</span>
                                }
                                else
                                {
                                    <span class="text-sm text-slate-500">No refill</span>
                                }
                            </td>
                            <td class="py-4 px-6">
                                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold @statusColor">
                                    @prescription.Status
                                </span>
                            </td>
                            <td class="py-4 px-6">
                                <div class="flex items-center space-x-2">
                                    <a asp-action="Details" asp-route-id="@prescription.Id" class="p-2 text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-lg" title="View details">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@prescription.Id" class="p-2 text-slate-600 hover:text-emerald-600 hover:bg-emerald-50 rounded-lg" title="Edit">
                                        <i class="fas fa-pen"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@prescription.Id" class="p-2 text-slate-600 hover:text-red-600 hover:bg-red-50 rounded-lg" title="Delete">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (!Model.Prescriptions.Any())
        {
            <div class="p-10 text-center">
                <div class="inline-flex h-16 w-16 items-center justify-center rounded-full bg-slate-100 text-slate-500 mb-4">
                    <i class="fas fa-prescription-bottle-alt text-2xl"></i>
                </div>
                <h3 class="text-lg font-semibold text-slate-900 mb-2">No prescriptions found</h3>
                <p class="text-slate-600 mb-4">Start by adding a new prescription.</p>
                <button class="gradient-primary text-white px-6 py-3 rounded-lg font-medium hover:shadow-lg transition-shadow inline-flex items-center">
                    <i class="fas fa-plus mr-2"></i>
                    Add Prescription
                </button>
            </div>
        }
        else if (ViewData["PageNumber"] != null)
        {
            <div class="p-4 border-t border-slate-200 flex items-center justify-between">
                <p class="text-sm text-slate-600">
                    Page @ViewData["PageNumber"] of @ViewData["PageCount"] (@ViewData["TotalItemCount"] total)
                </p>
                <div class="flex items-center space-x-2">
                    @if ((bool)(ViewData["HasPreviousPage"] ?? false))
                    {
                        <a href="?page=@((int)(ViewData["PageNumber"] ?? 1) - 1)" class="px-3 py-2 border border-slate-300 rounded-lg text-sm font-medium text-slate-700 hover:bg-slate-50">
                            Previous
                        </a>
                    }
                    @if ((bool)(ViewData["HasNextPage"] ?? false))
                    {
                        <a href="?page=@((int)(ViewData["PageNumber"] ?? 1) + 1)" class="px-3 py-2 border border-slate-300 rounded-lg text-sm font-medium text-slate-700 hover:bg-slate-50">
                            Next
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

<script>
    document.getElementById('prescriptionSearch')?.addEventListener('input', function () {
        const term = this.value.toLowerCase();
        document.querySelectorAll('tbody tr').forEach(row => {
            row.style.display = row.textContent.toLowerCase().includes(term) ? '' : 'none';
        });
    });
</script>
